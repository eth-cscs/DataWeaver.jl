# Reading one variable with a pre-configured XML file

```@eval
Main.mdinclude(joinpath(Main.EXAMPLEROOT, "read_test.jl"))
```
The corresponding file can be found [here](../../../assets/examples/read_test.jl).
